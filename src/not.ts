/**
 * A function that returns the `!` of its argument.
 * ```js
 * not(true) //=> false
 * not(false) //=> true
 * not(0) //=> true
 * not(1) //=> false
 * not('') //=> true
 * not('a') //=> false
 * ```
 */
const not = (operand: any): boolean => !operand

export default not
